#Directories
{   pwd
    mkdir
}
#Listing Contents and Understanding Permissions
{
    ls
    ls -l
    ls -lh
    ls -l --human-readable
    ls -al
}
#Navigating the Filesystem
{
    cd *file
    cd /
    touch *text
    echo 
    cat 
    nano 
    mv 
    cp 
    history
    curl 
    wget
    rm
    rmdir
    rm -r
    exit
}

#force your root filesystem to remount as rw
{
    mount -o remount,rw /
}